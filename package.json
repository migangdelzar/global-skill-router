{
  "name": "global-skill-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.2",
  "scripts": {
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "bootstrap": "bun run build && node scripts/bootstrap-global.mjs",
    "router": "bun run build && node bin/skill-router.mjs"
  },
  "engines": {
    "bun": ">=1.4.2 <1.5.0",
    "node": ">=20.0.0 <21.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
